# Copy to .env (gitignored) in this folder. Loaded automatically before tests run.
# Do not wrap the value in quotes unless the password contains spaces.
VISIONPAINT_TEST_PGADMIN=Host=127.0.0.1;Port=5432;Username=postgres;Password=your-password;Database=postgres
